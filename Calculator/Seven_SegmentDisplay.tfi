


// Instantiate the module
Seven_SegmentDisplay instance_name (
    .displayed_num(displayed_num), 
    .clk(clk), 
    .clr(clr), 
    .numberbox_out(numberbox_out), 
    .anode_activate(anode_activate)
    );


